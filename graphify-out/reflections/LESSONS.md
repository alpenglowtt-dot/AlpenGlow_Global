# Lessons

_Auto-generated by `graphify reflect` from 6 session memories in graphify-out/memory/. Deterministic; no LLM. Use for orientation — verify before relying, and revisit dead ends if the code has changed since._

## Summary

- 3 useful · 0 dead ends · 3 corrected · 0 unmarked

## Lessons

**Tentative** — useful in fewer than 2 results; verify before relying.

- `FOLLOWUP_WRITABLE` (1× useful)
- `safeEqual()` (1× useful)
- `initTripPlanner()` (1× useful)

**Contested** — mixed signals; recency decides.

- `crm/index.ts` — 2× useful, 1× dead end/corrected → recency leans **useful** (latest 2026-07-21)
- `submit-lead/index.ts` — 1× useful, 1× dead end/corrected → recency leans **useful** (latest 2026-07-21)
- `LEAD_WRITABLE` — 1× useful, 1× dead end/corrected → recency leans **useful** (latest 2026-07-21)
- `_tpCheckBothVerified()` — 1× useful, 1× dead end/corrected → recency leans **useful** (latest 2026-07-21)
- `compass.js` — 1× useful, 1× dead end/corrected → recency leans **dead end** (latest 2026-07-21)

**Corrections** — do these differently.

- "How does OTP verification connect to CRM lead creation?" → The graph shows only file-contains-function edges here; the real answer requires reading api.js, submit-lead/index.ts, crm/index.ts, and trip-planner.js directly, since the connection is an HTTP fetch call the AST parser cannot represent as an edge.
- "Why did the WhatsApp OTP template keep failing with Meta API errors 132001 and 132018?" → Stop guessing Meta API formats from documentation memory; add console.error logging of the full raw error_data.details field and read Meta actual response text, which named the exact required format directly.
- "Why does the graph barely represent compass.html even though it has hundreds of lines of real logic?" → For any HTML file with inline script logic in this project, skip graphify entirely and read the file directly, since the extractor does not parse inline scripts inside HTML.

## By topic

### Community 1

**Tentative** — useful in fewer than 2 results; verify before relying.

- `_tpCheckBothVerified()` (1× useful)
- `compass.js` (1× useful)
- `initTripPlanner()` (1× useful)

### Community 14

**Tentative** — useful in fewer than 2 results; verify before relying.

- `crm/index.ts` (1× useful)
- `submit-lead/index.ts` (1× useful)

### Community 23

**Corrections** — do these differently.

- "Why does the graph barely represent compass.html even though it has hundreds of lines of real logic?" → For any HTML file with inline script logic in this project, skip graphify entirely and read the file directly, since the extractor does not parse inline scripts inside HTML.

### Community 3

**Corrections** — do these differently.

- "How does OTP verification connect to CRM lead creation?" → The graph shows only file-contains-function edges here; the real answer requires reading api.js, submit-lead/index.ts, crm/index.ts, and trip-planner.js directly, since the connection is an HTTP fetch call the AST parser cannot represent as an edge.

### Community 6

**Tentative** — useful in fewer than 2 results; verify before relying.

- `FOLLOWUP_WRITABLE` (1× useful)
- `LEAD_WRITABLE` (1× useful)
- `crm/index.ts` (1× useful)
- `safeEqual()` (1× useful)

### Community 7

**Corrections** — do these differently.

- "Why did the WhatsApp OTP template keep failing with Meta API errors 132001 and 132018?" → Stop guessing Meta API formats from documentation memory; add console.error logging of the full raw error_data.details field and read Meta actual response text, which named the exact required format directly.
